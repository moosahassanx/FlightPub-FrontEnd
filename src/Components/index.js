// Used to export all component files, cleans up multiple imports in page files

import NavBar from './NavBar';
import CardCarousel from './CardCarousel';
import Footer from './Footer';
import CustomSlide from './CustomSlide';

export {
    NavBar,
    CustomSlide,
    CardCarousel,
    Footer,
}
